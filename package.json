{
  "name": "starvl-railway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix starvl-api && npm install --prefix starvl-app && npm run build --prefix starvl-app",
    "start": "npm start --prefix starvl-api"
  },
  "engines": {
    "node": "20.x"
  }
}
